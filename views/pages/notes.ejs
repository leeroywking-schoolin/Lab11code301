<% dbResults.results.forEach(book => { %>
  <div class="booksDiv">
    <p><img src="<%= book.image_url %>"></p>
    <p>
      <%= book.title %>
    </p>
    <p>
      <%= book.author %>
    </p>
    <p class="description">
      <%= book.description %>
    </p>
  </div>
  <% }); %>